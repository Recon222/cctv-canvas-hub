//! Shared types used across multiple features.
//!
//! Feature-specific types live in their respective `features/<name>/types/` modules.
//! Only types needed by multiple features or by the app setup belong here.

/// Default shortcut for the quick pane
pub const DEFAULT_QUICK_PANE_SHORTCUT: &str = "CommandOrControl+Shift+.";
