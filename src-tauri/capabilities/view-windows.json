{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "view-windows-capability",
  "description": "Minimal capability for the pop-out view windows (T9: secondaries are never granted main's permission set — events for the token handshake, logging, locale detection, and open-externally only)",
  "windows": ["view-case", "view-map"],
  "permissions": [
    "core:event:default",
    "core:event:allow-emit",
    "log:default",
    "os:default",
    "opener:default"
  ]
}
