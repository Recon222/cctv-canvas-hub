/**
 * Example Feature - Types
 *
 * Feature-scoped types live here.
 * If a type is needed by 2+ features, promote it to lib/types/.
 */

export interface ExampleData {
  id: string
  name: string
  value: number
}
