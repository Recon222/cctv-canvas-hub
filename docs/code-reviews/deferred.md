# Deferred Items Ledger

Human-readable register of everything consciously deferred — from code reviews, plan reviews, and implementation. Each entry names its trigger: the milestone/event at which it must be picked up or consciously re-deferred. Remove entries when done (git history keeps the record).

| # | Item | Origin | Trigger / pick up at | Notes |
| - | ---- | ------ | -------------------- | ----- |
| D1 | `secure-vault`: `seal`'s encrypt-failure maps to `VaultError::Corrupt` (an open-side name) | PR #2 review, LOW (rust-reviewer) | Only if the seal path ever gains reachable failure modes | Unreachable below ~64 GiB plaintext; cosmetic. Optional `SealFailed` variant. |
| D2 | Schema gate: transient network error during the gate check drops to `signed-out` instead of a retryable gate state | PR #2 review, INFO (database-reviewer) | **Re-deferred at 6.2 (third window):** M7 Phase 7.2/7.3 (secondary contexts replay bootstrap-like flows), or the first live report of a relaunch-during-blip forcing re-sign-in | Consciously passed at M2/M5 and again at 6.2: the gate check runs during `booting`, BEFORE the board/health machinery mounts — representing "gate couldn't run" honestly needs a session-state addition (+ screen), not a health mark, and M6's binding 6.2 row scopes only the wake path. Cost of the gap: one extra password entry on a kiosk relaunch during a blip. If picked up, prefer a bounded retry of `checkSchemaGate` in bootstrap over a new state. |
| D6 | ui-store LEFT-sidebar visibility state + its palette/menu toggles are dormant | Plan AD9 | `/cleanup` pass post-V1 | Neither template sidebar file renders anywhere — A1's NavRail and A2's ProcessPanel are bespoke board-tree components in those chrome positions (there is no "slot" render). **Right side exited dormancy at 6.3 (as-built):** the ProcessPanel reuses `rightSidebarVisible` + the Cmd/Ctrl+2 shortcut + both palette commands as its expanded/collapsed state. Only the LEFT-side visibility plumbing and toggles remain deferred. |
| D8 | Per-thumbnail signed-URL creation (no batch) | Plan 4.1A known ceiling | Dense cases (~100+ media tiles per case) | `createSignedUrls` (per-case batch) is the pinned upgrade path. |
| D9 | Left-rail view switcher + three-view IA + multi-window (Amendment A1) | Product decision 2026-07-19 | **Docs amended** (AD12–AD14, M7). Build: rail + CasesView at M2 (2.4), dashboard M5, pop-outs M7 (A2: diagnostics is the in-main ProcessPanel at 6.3, not a window) | Rail is nav chrome, not an info panel. Claude-design package covers the three views + rail, never multi-window. |
| D10 | In-session supabase MCP connection may point at the old audited project until the session restarts | M1 implementation note | Any new session (auto-resolves) | Stored config already pins canvas-hub-dev (`fniyjetrgqsamqvkcrxg`); verify with `get_project_url` before relying on MCP schema inspection. |
| D11 | Crash-mid-write leaks orphan `{cloud-config.json,session.vault}.{pid}.{n}.tmp` files (no startup sweep) | PR #2 fix-delta, LOW (rust-reviewer) | Next touch of `cloud_session/services/mod.rs`, or if orphans are ever observed | ~4 KB files under a rare trigger; the unique-name fix that causes this is correct. Optional glob-sweep at startup. |
| D13 | `getItem` of the `-user` sibling key would trip the single-key loud-fail if `auth.userStorage` were ever configured | PR #2 fix-delta, INFO (database-reviewer) | Any future work configuring `auth.userStorage` | Unreachable today (`_saveSession` never writes `-user` without userStorage) and fails closed if reached. If userStorage arrives, scope the sibling no-op to `getItem` too. |
| D18 | `mediaPlayerIncluded` is `boolean` on the wire; shipped `src/lib/supabase/database-types.ts:78` still declares `string` | PR #5 plan review, MEDIUM (reality + database) | First consumption of `exportInformation` (M4/M5 expanded-card detail) | Doc 01 §5.1 carries the ⚠. A `string`-typed boolean renders as **nothing** in React — the field silently vanishes with a green gate; flip the shipped type when the first surface consumes it. (PR #6 L2's `MediaKind` rider was adopted at the PR #7 fix round — union on the view-model, unknown-bucket normalization at the mapper; only the `mediaPlayerIncluded` flip remains on this row.) |
| D19 | Server-side session revocation is not detected until token refresh — bound: **≤ access-token TTL (~1 h)**. A revoked-but-unexpired stateless JWT keeps realtime + REST working (PostgREST validates signature + `exp` locally; no admin path — `signOut global`, user deletion, `banned_until` — pushes a socket disconnect) | M6 live smoke, item 7; adjudicated LOW/accept by the PR #9 review (silent + database lanes concur) | M7 auth-topology work (secondaries + `onAuthStateChange` wiring), OR if a coordinator-revocation-must-sign-out-promptly requirement is ever stated | The board stays LIVE (never silently stale — the data is genuinely current) and a fully-revoked session self-corrects honestly at the next refresh. **Genuinely-silent sub-case (database lane):** agency MEMBERSHIP revoked mid-session while the JWT stays valid ⇒ REST reconcile returns ok+empty, realtime stays subscribed delivering nothing ⇒ green dot over an empty board, same ≤1 h bound. Inherent to stateless-JWT + agency-wide reads, NOT introduced by M6; the only faster-revocation lever is a shorter server-side access-token TTL, not client code. |
| D21 | M1 self-close residual: the ended secondary closes its own window via `commands.closeViewWindow` after teardown; if that IPC *genuinely fails* (a true IPC error — "already gone" returns `Ok`), the window lingers on the terminal screen and the original M1 focus-dead-window wart can recur on re-sign-in (`open_view_window`'s focus-if-open finds the still-alive Rust window even though main's JS registry was cleared by `resetViewWindows` on board unmount) | PR #10 fix-delta, LOW (silent-failure-hunter) | Next touch of the `view_windows` teardown path, or if an orphaned ended window is ever observed live | **Strictly narrower than the original M1 and zero data risk** — the token is discarded, the cache purged, and `endedRef` blocks any revival, so a lingering window is inert. Belt-and-braces fix: have **main** also call `closeViewWindow` for both views on session exit, so closure doesn't depend solely on the secondary's IPC succeeding. Optional; the reopen path is otherwise fully fixed. |
| D20 | PR #9 deferred LOWs (consolidated): **L1** `catchUp` has no in-flight/dedup guard (`online` + `visibilitychange` fire together on wake ⇒ two concurrent runs — benign, TanStack + supabase-js both coalesce) · **L4** `ProcessPanelRow.lane` is a vestigial discriminant (only `'system'` constructed; ACTIVITY is a ReactNode slot) · **L5** `VaultStatus` mtime/present coupling not type-enforced (sole constructor enforces it) · **L6** `ansiParser` `chunksToLines` is test-oracle-only (the vtEngine golden-parity suite) · **L7** a wedged log-tail read freezes the log sub-lane with no staleness signal (self-heals on lane toggle) · **L8** `vaultGet` runs on every focus/online wake; transient keychain unavailability reads as no-session (pre-M6 behavior, amplified exposure) · **N1 (fix-delta)** `persistLockedFlag` is fire-and-forget with a log-only catch: if `lock()` flips in-memory but the disk write fails AND a reload lands before any later successful persist, bootstrap reads stale `locked:false` and restores to `active` — ACCEPTED: fire-and-forget is correct (the wall must go up instantly; blocking it on I/O is worse), `atomic_write` rules out flag corruption, and an unattended kiosk can't act on a toast; recorded so a future reader doesn't re-discover it as a bug | PR #9 review, LOW ×6 + fix-delta N1 | L1: if a wake-storm is ever observed · L4: next panel touch (drop or narrow to `'system'`) · L6: `/cleanup` pass (or relocate under `__tests__/`) · L7: only if observed live · L5/L8: conscious trades, revisit on their surfaces' next touch · N1: revisit only if a durable lock-state store is ever added | None blocks the milestone; L2 (broad catch → sign-out) was FIXED in the PR #9 round, not deferred. |
