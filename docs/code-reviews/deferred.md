# Deferred Items Ledger

Human-readable register of everything consciously deferred — from code reviews, plan reviews, and implementation. Each entry names its trigger: the milestone/event at which it must be picked up or consciously re-deferred. Remove entries when done (git history keeps the record).

| # | Item | Origin | Trigger / pick up at | Notes |
| - | ---- | ------ | -------------------- | ----- |
| D1 | `secure-vault`: `seal`'s encrypt-failure maps to `VaultError::Corrupt` (an open-side name) | PR #2 review, LOW (rust-reviewer) | Only if the seal path ever gains reachable failure modes | Unreachable below ~64 GiB plaintext; cosmetic. Optional `SealFailed` variant. |
| D2 | Schema gate: transient network error during the gate check drops to `signed-out` instead of a retryable gate state | PR #2 review, INFO (database-reviewer) | M2/M5 connection-health work | Fail-closed and recoverable — acceptable for M1. Revisit once the health store exists to represent "gate check couldn't run". |
| D3 | `reauthenticate()` collapses wrong-password vs network-unreachable into `false` | PR #2 review, INFO (silent-failure-hunter) | M6 Phase 6.1 (LockOverlay wiring — its first live caller) | Reuse the `ProbeRejectedError`/`ProbeUnreachableError` distinction so the lock screen can say which it was. |
| D6 | ui-store sidebar-visibility state + palette/menu sidebar toggles are dormant | Plan AD9 | `/cleanup` pass post-V1 | Neither template sidebar file renders anywhere — A1's NavRail and A2's ProcessPanel are bespoke board-tree components in those chrome positions (there is no "slot" render). **Right side exits dormancy at 6.3:** the panel reuses `rightSidebarVisible` + its shortcut/palette commands as its expanded/collapsed state, default flipped open (A2 fix round). Only the LEFT-side visibility plumbing and toggles remain deferred. |
| D8 | Per-thumbnail signed-URL creation (no batch) | Plan 4.1A known ceiling | Dense cases (~100+ media tiles per case) | `createSignedUrls` (per-case batch) is the pinned upgrade path. |
| D9 | Left-rail view switcher + three-view IA + multi-window (Amendment A1) | Product decision 2026-07-19 | **Docs amended** (AD12–AD14, M7). Build: rail + CasesView at M2 (2.4), dashboard M5, pop-outs M7 (A2: diagnostics is the in-main ProcessPanel at 6.3, not a window) | Rail is nav chrome, not an info panel. Claude-design package covers the three views + rail, never multi-window. |
| D10 | In-session supabase MCP connection may point at the old audited project until the session restarts | M1 implementation note | Any new session (auto-resolves) | Stored config already pins canvas-hub-dev (`fniyjetrgqsamqvkcrxg`); verify with `get_project_url` before relying on MCP schema inspection. |
| D11 | Crash-mid-write leaks orphan `{cloud-config.json,session.vault}.{pid}.{n}.tmp` files (no startup sweep) | PR #2 fix-delta, LOW (rust-reviewer) | Next touch of `cloud_session/services/mod.rs`, or if orphans are ever observed | ~4 KB files under a rare trigger; the unique-name fix that causes this is correct. Optional glob-sweep at startup. |
| D13 | `getItem` of the `-user` sibling key would trip the single-key loud-fail if `auth.userStorage` were ever configured | PR #2 fix-delta, INFO (database-reviewer) | Any future work configuring `auth.userStorage` | Unreachable today (`_saveSession` never writes `-user` without userStorage) and fails closed if reached. If userStorage arrives, scope the sibling no-op to `getItem` too. |
| D14 | `canPoll` (health-store) and the `'media-new'` ActivityKind have no M2 consumer | PR #4 fix-delta, deferral audit | M4 (`useMediaPolling` polling gate · media attention events) | **Do not delete in `/cleanup`** — knip will flag both as unused exports until their named M4 consumers land; deleting and re-adding costs the same, so they stay. |
| D15 | M5 entry criterion: the health indicator is load-bearing for M2's "stale-visible beats blank" product call | PR #4 fix-delta (silent-failure-hunter) | M5 kickoff — before any other M5 work | M2 computes degradation correctly and renders it **nowhere**: a persistent fetch failure leaves the old board on the wall with no on-screen signal until M5's indicator lands. The criterion stands even with `RECONCILE_MS` now below `STALE_AFTER_MS` (that fixed the false-STALE arithmetic, not the missing surface). |
| D16 | Location-card selection a11y: single-select model (`role="option"`/`aria-selected` + `role="listbox"` on the stack) | PR #4 fix-delta round 2, LOW (typescript-reviewer) | M3 Phase 3.4 (`LocationCardStack.tsx` + `LocationCard.tsx` — the DOM that can carry the roles) | `aria-pressed` was correctly dropped in M2 (set-only selection); once selection drives fly-to, AT must perceive it — and the accurate model is single-select, not a re-added toggle. Re-homed 3.3B → 3.4A (A2 fix round): `MarkerLayer` renders WebGL output with no per-location DOM node, and splitting listbox/options across subtrees is invalid ARIA. |
| D18 | `mediaPlayerIncluded` is `boolean` on the wire; shipped `src/lib/supabase/database-types.ts:78` still declares `string` | PR #5 plan review, MEDIUM (reality + database) | First consumption of `exportInformation` (M4/M5 expanded-card detail) | Doc 01 §5.1 carries the ⚠. A `string`-typed boolean renders as **nothing** in React — the field silently vanishes with a green gate; flip the shipped type when the first surface consumes it. |
| D17 | Two untested guards: `updated_at` null-guard in `toCanvassCase` (mutation-verified revertible) and `cancelRefetch: false` on the counts/cases invalidations | PR #4 fix-delta round 2, LOW (pr-test-analyzer) | M3 first touch of `realtime.test.ts` / `useCaseRealtime` | Both behaviors are correct and shipped; both can be silently reverted with a green suite. One test arm each (`updated_at: null` event; an options spy). |
